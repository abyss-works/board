apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: community-board
  namespace: stage
spec:
  ingressClassName: nginx
  rules:
  - host: "board.abyssworks.dev"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: community-board
            port:
              number: 80
